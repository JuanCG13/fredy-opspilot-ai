# Optional: the public Vercel demo runs with seeded/mock data.
# Add an OpenAI-compatible key only if you extend the backend AI provider locally.
AI_PROVIDER_BASE_URL=
AI_PROVIDER_API_KEY=
DATABASE_URL=sqlite:///./opspilot_demo.db
